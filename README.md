# tmcctl
TMC device management utility (link to tmc repository: https://github.com/drowsymike/tmc)  

# Build
Open your terminal, enter:  
`gcc -Wall tmcctl.c -o tmcctl`  
Then:  
`./tmcctl [args]`  

# About args
See info about tmcctl args by entering in the terminal:  
`./tmcctl --help`